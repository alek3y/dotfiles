py3status {
	separator_block_width = 30
}

order += "audio"
order += "brightness"
order += "time"

audio {
	natural_scrolling = ${NATURAL_SCROLLING}
}

brightness {
	natural_scrolling = ${NATURAL_SCROLLING}
}

time {
	format = "%time"
	format_time = "%H:%M %d/%m"
}
